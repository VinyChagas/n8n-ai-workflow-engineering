# Workflows

- `examples/` — generic sample workflows safe to publish
- Root `*.json` — your local exports (optional; not required for the OSS toolkit)

Configure `WORKFLOW_DIRECTORY` in `.env` if you keep exports elsewhere.
